# Key Vault
KEY_VAULT_URL=https://<your-vault-name>.vault.azure.net/

# Local dev fallback (do NOT commit real secret)
JWT_SECRET_KEY=dev-only-super-secret
APP_NAME=smart-inventory-api
JWT_AUDIENCE=smart-inventory-clients
